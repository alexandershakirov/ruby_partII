<%= form_for(@tag) do |f| %>
    <%= render 'common/errors', object: @tag %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
